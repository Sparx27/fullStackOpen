const ErrMessage = ({ message }) => <div style={{padding: "8px", background: "rgb(237, 170, 170)", marginBottom: "8px"}}>
  <p style={{color: 'red', fontSize: "20px", fontWeight: "bold"}}>{message}</p>
</div>

export default ErrMessage